# json

```
{"key1":obj1,"key2":obj2}
[obj1,obj2]

let people = {name:"aa",show: function(age){ this.name + age )}};
people.show(10);

并列数据之间用,
映射用:
并列数据集合（数组）用[]
映射的集合（对象）用大{}

JSON.parse()      将JSON 字符串转换为 JavaScript 对象。
JSON.stringify()  将 JavaScript 值转换为 JSON 字符串。
```
