# RichTextBox

```
<RichTextBox Name="rr"  VerticalScrollBarVisibility="Auto"/>

rr.Document.Blocks.Add(new Paragraph(new Run("bbbb") { Foreground = Brushes.Green }));
rr.ScrollToEnd();

rr.Document.Blocks.Clear();
```
