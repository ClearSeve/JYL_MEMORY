# 递归

```
递归函数可以将函数中的数据分成若干分支，其中一定包含一种不需要调用递归函数的情况（结束深层次调用情况），剩余则是需要将数据进行深层次调用递归函数情况

              Fun
  结果1          结果temp1（调用Fun）
          结果2           结果temp2（调用Fun）
                      结果3               结果4

递归求阶乘
double fun(int i){
  if(i<=1) return 1.0;
  return i*fun(i-1);
}
```
