# 函数重载

```
函数重载：用同一作用域中，函数名相同，参数列表不同（个数，顺序，类型）的函数构成重载，返回值不构成重载。
因为gcc编译时不会给函数改名，g++编译时根据名字和参数改名
在c++代码中的函数名前加extern “C”可以在编译时不改变函数名，这样就可以让c与c++互调函数
1）c调用c++函数：
     g++   -c  tc.cpp  生成  tc.o文件
     gcc    t.c   tc.o  -lstd++
2）c++调用c的函数
     gcc   -c  t.c    生成t.o文件
     g++  tc.cpp  t.o
```