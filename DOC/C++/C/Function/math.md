# math

```
z=pow(x,y);  //计算x的y次方，X与z必须同时是float或double。
z = pow((float)x,(float)1.0/3); //计算x的三次开方

int a=(int)sqrt((double)25);  //对25开平方，得到5。

abs(x)；    //求绝对值。

float  a=sin(3.14/2);  //参数是弧度，计算sin90度的值。

ceil向上取整，floor向下取整
```
